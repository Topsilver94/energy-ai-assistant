# verify/ — 全链路回归验证脚本

Playwright 驱动真实页面（headless Chromium），对三大模块与配置中心做端到端回归。
不依赖项目自身依赖（复用 npx 缓存里的 playwright），不侵入 `src/`。

## 脚本清单

| 脚本 | 覆盖 |
|---|---|
| `m1.cjs` | 模块②：表单→组合总账→分项表→敏感性；数值与手算对照（广东 光伏2000kW+储能1000kWh ⇒ 投资685万/IRR 27.3%/回收3.6年/碳减排1239.2t——2026-09 capex 外部锚定换版：光伏 3.5→3.0 元/W、储能 1200→850 元/kWh（`verify/external-anchors.md`）；储能两充两放等效 1.5 次/日 × 工程修正 DoD 0.9 × 330 天 → 445,500 kWh × 2026年9月 广东峰谷价差 1.3529 − 充电损耗 60,750 kWh × 0.3 元 → 毛收益 58.45万/年，IRR 66.4%/回收 1.5 年；无需量快照 → 需量收益不计）；规模为 0 的校验 toast |
| `m2.cjs` | 模块①：既有路径（对标/潜力/评级/3条措施/推荐列表/热力图）、储能双口径定容（推定变压器 1,600 kVA × 25% × 2h 与负荷口径取短板 → 800 kWh）+ 需量基数推定行（负荷率法 676 kW × 变压器法 800 kW 取短板 → 676 kW，随采纳快照入②）+ 广东两充两放运行模式行（分时结构判定）+ 布置红线不出现于推荐侧（仅模块③ 出）、一键填入模块②、新建路径（设计校核、不输出节能潜力）、商场集中供冷双口径（占比0.9 → 供冷面积1.8万㎡ · 折算3600kW，负荷指标 200 W/㎡ 手册中值；客户侧 TCO 对比行：分体 48.5（电费12.5+维保6+折旧30）vs 集中 37.5 元/㎡·a → 年省 11.0 元/㎡ 23%）、新建广东分省配建档（车位120 × 20% 省档 → 24 枪 12 桩；既有仍走 10% 底线=场景G）、工业厂房供冷如实降级（触发依据含「需工艺负荷资料复核」，非置信度徽章）、屋面类型分支（坡屋面728kW · 新建BIPV满铺1008kW · 平屋面典型=场景A 800kW）、屋面面积实填（办公4,000㎡·平屋面 → 4,000×0.8障碍折减=3,200㎡×0.1=320kW，换算链入触发依据）、电费未知兜底（面积口径典型强度115→潜力13.0% 预估标注 · 变压器口径1100kVA取短板强度108.4 · 月均电费16万转实测强度128.0） |
| `load-curve.cjs` | 模块① 负荷曲线上传（既有专属）：合成全年 15 分钟曲线 CSV → 摘要卡统计量（年电量 563 万kWh · 最大需量 785 kW · 负荷率 82% · 15 分钟 × 35,040 点，期望值由生成式反解）；曲线口径优先于电费反推（年电量 562.8 万kWh，电费反推值 666.7 万kWh 不得出现）；实测需量传入储能推荐（触发依据含「需量基数实测」与 785 kW）；**口径溯源贯通模块③**（填入模块② 测算 → 生成方案，需量句须写「实测（负荷曲线口径）需量基数」，不得回落「推定」）；移除曲线回退电费口径；新建分支不渲染上传位；不足一天的无效文件 amber 拦截且不落摘要；**上传说明文案契约**（须写明「时间 + 功率」两列 / 单位 kW / 功率放末列——解析器取行末数值且不读单位，这两条边界不写在客户第一眼处就会静默算错） |
| `m3.cjs` | 模块③：锁定态、未填 Key 本地模板降级（含年代侧重注入）、401 分型提示、Key 不落 localStorage、剪贴板复制 |
| `m4.cjs` | 模块③：真实 GLM-5 流式生成（自动读 `verify/aikey.txt` 或 `GLM_KEY` 环境变量，Key 不进仓库不落盘） |
| `m5.cjs` | 双界面切换不丢状态、专家参数/电力市场数据抽屉（广东电价）改系数→组合结果自动重算、工程估算参考抽屉（屋面系数）改值→模块① 推荐规模自动重算、分抽屉恢复默认（工程抽屉恢复不影响电力抽屉改动，逐抽屉还原收尾） |
| `era-dates.cjs` | 守护：年份滑杆上限随当前年自动前滚、既有结果标准代际侧重行（2005 前 / 节能 50% / 节能 65% 三档）、≥2022 年 GB 55015 光伏余量提示双出口（年代行 + 光伏触发依据）、新建不输出年代行、储能触发依据月份随 SPREAD_AS_OF 常量渲染（动态读取源文件，换月不破） |
| `mobile-check.cjs` | 手机端视口守护（390×844 iPhone 口径，**硬断言 + 失败即 exit 1**）：页头不溢出 + 四入口图标化折叠、01/02/03 移动分页并入 header 随顶栏常驻（WorkNav 隐藏）、三页无全局横向溢出、省份索引栏（①② 同构）展开可用、STEP2/3 分项明细表在卡片内横滑看全列（量容器 scrollWidth>clientWidth，列宽不压扁、不外溢文档）、本地模板生成后导出 PDF 点击有引导反馈、演示模式单列/三列（等宽 + 不越右缘）、**演示模式定位书签不遮信息**（收起态宽度 ≤ 内容左缘留白，390 与 1280 双档量）+ 点 STEP③ 落到该列顶部（= scroll-margin，顶栏 65px 未压住）且报告内部滚动归零 + **触摸端点书签后该列常驻亮绿描边**（无 hover 设备的聚焦反馈，与桌面端悬浮跟随互为一对）；默认连 :5175（BASE_URL 可覆盖） |
| `demo-layout.cjs` | 演示模式版式与导航守护（1440×900）：① 三列 Card 底边对齐；② 敏感性表窄列横滑 + 三行制不暴涨 + 变量列 sticky（横滑后左缘偏移 ≤1px）；③ 报告区吃满等高列且**被行高约束**（content/viewport > 1.05 即证明报告不参与决定行高，结构性判据不随列高浮动）；④ 执行摘要卡 2×2；⑤ 模块②/报告两处分项明细表首列 sticky；⑥ 台账快照剪贴板 TSV；⑦ **演示模式定位书签**——与工作模式书签互斥渲染、内容左右留白对称（书签不占位）、落点 = 列顶（scroll-margin 预留顶栏）、STEP③ 报告内部归零、**聚焦描边悬浮跟随**（点击不留常驻态 / 悬停哪列哪列亮 / 换列时上一列熄灭 / 移开即灭）、不引入横向溢出；⑧ 演示窄列下推荐卡标题单行不折行（行数按实测行高换算）；⑨ 空态跳 STEP③ 后「生成方案报告」CTA 落在首屏内且位于**首屏中部**（视口 45–68% 高——演示卡片被拉伸到整行高，贴顶与居中都不可取）；外加 <lg 堆叠态报告区不塌陷 |
| `plan-compare.cjs` | 模块② 方案比选（档位由① 推荐派生）：②未测算时不出表（不抢在结果之前出现）、仅②测算时只出「当前配置」一档 + 如实提示其余档位待① 生成、①完成后档位数 = 推荐项数+当前配置+合计且**档序 单项… → 当前配置 → 合计居末**、各单项档档位名与①等级与 STEP1 推荐列表**逐字一致且保持①列表原序**（引擎 score 是四把不同量纲的尺子，不得据其重排）、汇总两档不标①等级、合计档投资 = 各单项投资之和（展示值反解）、当前配置档与组合总账数据卡逐格同源、口径说明行写明①诊断省份、首列 sticky、**省份随「填入模块②」携带**（携带后② 重挂即按新省份重算、未重测时改表单省份不改口径行归属、重测后如实报跨省）、**当前配置构成一系统一行**（行数 = 已启用系统数，不拼成一行撑宽「档位」列）、**表单与那次测算不一致时该行挂「表单已改 · 重测后刷新」**（一致时不挂，重测后消失） |
| `heatmap-layout.cjs` | 工作模式诊断页热力图几何（一次性目检辅助，不进 CI 主链路）：说明句在表格下方、与上方表单间距 ≥ 24px、表格底端 ≈ 右列最后一张推荐卡底端（而非「填入模块②」按钮行） |
| `reportdoc.cjs` | 模块③ 报告版式外壳（本地降级路径）：报告头/执行摘要/分项明细/敏感性/参数附表/报告尾、五段正文 h2 逐字契约、h1 与 table 数量契约、无 NaN/undefined 泄漏 + **上游漂移守护**（刚生成完不误报 / 改② 组合后提示点名「模块② 组合配置」并说明正文旧版式新 / 重新生成后提示消失） |
| `print-check.cjs` | 模块③ 打印版式（本地降级路径）：生成方案 → `@media print` 断言正式报告样式（白底黑字、隐藏导航与参数面板）+ 产出 A4 PDF |
| `readme-shots.cjs` | README 截图再生成（1440×900 @2x retina，演示数据就绪态）→ `docs/screenshots/`；界面布局变更后重跑 |
| `blank-check.cjs` | 白屏诊断工具（非断言）：加载页面收集 console / pageerror / 请求失败 / 根节点渲染情况——整页空白时先用它定责，不靠猜 |
| `anchors.mjs` | 离线数值锚点（常规套件成员；纯 Node ESM 直 import `src/` 计算层，全仓首个不读 DOM 文本的数值验证——`package.json` `"type":"module"` 使 `src/utils`、`src/data` 可直接 import）：A0 系数指纹（defaultConfig+recommendationRules 叶子路径结构指纹 + 锚点所用默认值数值钉；provinces 数值整体不钉——峰谷价差按月换版只改数值不动结构，换版不红、动结构必红）→ A1 IRR 闭式解【强】（合成 `lifetimeYears` 1/2/100 造单期/两期求根/永续极限三个代数精确解，与代码的二分迭代是两条路径；\|Δ\|≤1e-4 = 二分容差量级，1e-6 形 100% 误报）→ A2 单位哨兵【强】（系数设 10 的幂，走查 kW→W ×1000、万㎡→㎡ ×1e4、元→万元 ÷1e4、kWh→MWh ÷1000 整条换算链，精确 ===）→ A3 手推算术【中】（四系统分项 + 两系统合计按 CLAUDE.md §5 公式与默认值手推期望，与 m1 人工基线 820 万/22.2%/4.3 年互证）→ A4 需量锚点（315 kVA 门槛 / 月每 kVA 恰 260 → 九折价 27 无浮点漂移 / 削峰封顶 100/199/200/201/400 五点）→ A5 解析与档位边界（恒定·锯齿·96 点均值曲线的闭式反解 + 年电量按未舍入均值而 avgKw 是 1 位小数舍入值的陷阱钉、评级阈值压线 ±ε 探针、供冷面积档与新建充电桩档压线） |
| `numeric-audit.mjs` | 一次性数值网格审计（诊断件，不进 CI 主链路——同 `heatmap-layout.cjs` 定位；同样纯 Node ESM 离线跑）：6 省 × 4 系统规模档 × 3 需量（含 314/315 门槛两侧）+ 5 组合 = 324 组合，扫 11 类结构不变量——config 不可变 / 守恒律（重言式护栏，防合计口径将来改道）/ IRR 符号夹逼（NPV(irr∓2e-4) 异号，量纲无关）/ 单系统 total===item 逐位 / 线性齐次 ×2 逐位（乘 2 是 2 的幂，浮点严格成立）/ 需量分段封顶 / 敏感性接线（平坦轴集合 + 电价轴方向——抓扰动打错字段）/ NaN 泄漏（含逐个删可选系数走兜底路径）/ recommend→finance 联动（推荐卡 estimate 与直算逐位相等，跨模块单位串位唯一抓手）/ horizon 截断（合并流长度 = 最长寿命，且全寿命年金口径 NPV(irr) 明显 ≠0——后半句防止断言为空）/ 入参边界（形状异常抛错、无可测算返回 null）；**6 条已实测疑点钉成现状断言**（合计回收期用全寿命年净而 IRR 按寿命截断、IRR 为正却敏感性不适用、IRR 可为负、未收录省静默回退北京、既有建筑误挂「新建无负荷数据」文案、暂缓档默认系数不可达）——钉住 ≠ 认可，修复属口径轮：改后本节会红，红 = 行为已变需同步；文末另有模块默认值快照自检（脚本改系数必须深拷贝，浅拷贝写穿共享引用会让后续段落全成脏数——初版实测栽过：删广东 cyclesPerDay 让疑点① 打出 21.2% 脏数） |

## 运行

```bash
# 1. 起 dev server（脚本默认连 localhost:5175，可用 BASE_URL 覆盖）
npm run dev

# 2. 首次需装浏览器内核（默认 CDN 国内不通，必须走 npmmirror 镜像）
PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries/playwright npx playwright install chromium --only-shell

# 3. 跑脚本（playwright 包从 npx 缓存解析，无需安装）
NODE_PATH="C:/Users/hp/AppData/Local/npm-cache/_npx/e41f203b7505f1fb/node_modules" node verify/m1.cjs

# m4 需要真实 Key：把 Key 粘贴到 verify/aikey.txt 首行（文件已 gitignore），再直接跑
NODE_PATH=... node verify/m4.cjs   # 也可用 GLM_KEY 环境变量注入，二选一

# anchors.mjs / numeric-audit.mjs 例外：纯 Node ESM 直 import src/ 计算层，
# 不需要 dev server，也不需要 NODE_PATH（package.json "type":"module"）
node verify/anchors.mjs
node verify/numeric-audit.mjs
```

产出：`verify/out/*.json`（提取数值+控制台报错）、`verify/shots/*.png`（各阶段截图）。
两目录为可再生工件，已 gitignore；脚本本身随仓库提交。

## 判定口径

- 脚本退出码非 0 = 存在 console/page 错误或断言失败
- `out/*.json` 中的 `extracted` 字段用于人工比对预期值（README 表内即基准）
- 系数默认值改动会使历史基准失效，改 `data/coefficients.js` 后需同步更新本 README 的对照值
- **期望值的独立性分级（`anchors.mjs` 起引入，混着讲就会自欺）**：多数既有守护的期望值由被测代码同一公式反解（自洽——防回归有效，证正确无力；储能「容量×365 满充满放」高估约 20% 的真实事故发生时它们全绿）。锚点按证明力分三级：**强** = 闭式解（合成 config 造代数可解情形）+ 单位哨兵（系数设 10 的幂走查换算链），能证「代码实现错」；**中** = 按 CLAUDE.md §5 公式手推算术，只能证「实现相对文档漂移」；**观察** = `numeric-audit.mjs` 的网格扫描与疑点钉现状，是发现不是验证。锚点证明不了「文档公式/系数默认值贴合现实」——那靠参数级外部锚定（对照记录：`verify/external-anchors.md`，2026-09 首轮：排放因子逐位锚定、储能/光伏 capex 高估 48%/20–30% 已记录待换版、分省利用小时无公开权威披露如实标注）
- `m3-ai.cjs` 是 DeepSeek 时代留下的旧脚本（需 `DEEPSEEK_API_KEY` + 模型 `deepseek-v4-flash`）：`verify/aikey.txt` 现放的是 GLM Key，跑它必然在「AI 徽章」一条失败并回落本地模板（其余断言仍过）——**这是环境缺 Key，不是回归**，已用 `git stash` 基线核过。真实 AI 链路回归走 `m4.cjs`
